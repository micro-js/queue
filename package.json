{
  "name": "@f/queue",
  "description": "Simple function queue",
  "repository": "git://github.com/micro-js/queue.git",
  "version": "1.0.1",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/map-array": "^1.2.1"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
